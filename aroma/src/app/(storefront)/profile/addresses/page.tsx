import { AddressesPageClient } from '@/features/profile/AddressesPageClient'

export const metadata = { title: 'My Addresses — Aroma' }

export default function AddressesPage() {
  return <AddressesPageClient />
}
